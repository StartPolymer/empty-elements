# empty-elements

Repo contain empty elements for replace the elements in bower flat dependency tree. The elements are using in [Progressive Web App Template](https://github.com/StartPolymer/progressive-web-app-template).
